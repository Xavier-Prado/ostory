{% extends 'base.html.twig' %}

{% block title %}Editer la page{% endblock %}

{% block body %}
    <div class="my-5 pt-4">
        <div class="col-lg-6 mx-auto">
            <div class="text-center">
                <h1>Editer la page</h1>
                <a class="btn btn-info m-1 my-4 show--button" href="{{ path('app_page_index') }}">Revenir à la liste</a>
            </div>

            <div class="mx-1">
                {{ include('page/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
                <div class="float-end">
                    {{ include('page/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
