<form method="post" action="{{ path('app_choice_delete', {'id': choice.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ choice.id) }}">
    <button class="btn btn-danger col-2 m-1">Supprimer</button>
</form>
