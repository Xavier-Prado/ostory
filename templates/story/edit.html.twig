{% extends 'base.html.twig' %}

{% block title %}Modifier une histoire{% endblock %}

{% block body %}
    <div class="my-5 pt-4">
        <div class="col-lg-6 mx-auto">
            <div class="text-center">
                <h1>Modifier une histoire</h1>
                <a class="btn btn-info m-1 my-4 show--button" href="{{ path('app_story_index') }}">Revenir à la liste</a>
            </div>

            <div class="mx-1">
                {{ include('story/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
                <div class="float-end">
                    {{ include('story/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
